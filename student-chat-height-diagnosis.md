# تشخيص عدم ظهور ارتفاع الدردشة — 2026-08-17

المشكلة ليست أن قاعدة `height: 400px` وُضعت على عنصر غير صحيح فقط؛ القاعدة تصل إلى `.student-chat-panel`، لكن الإطار لا يستطيع الظهور بارتفاعه الكامل بسبب تخطيط الغلاف العمودي.

في الوضع العمودي، `html` و`body` لهما `height: 100%` و`overflow: hidden`. كذلك `.student-live-page` مثبت بـ `position: fixed; inset: 0; height: 100svh; overflow: hidden`، و`.viewer-shell` له `height: 100%; overflow: hidden`، و`.viewer-stage` له `height: 100%; overflow: hidden; min-height: 0`.

لذلك فإن صف الدردشة الثابت `400px` داخل شبكة `.viewer-stage` يتجاوز المساحة المتبقية بعد الهيدر والفيديو وشريط الأزرار، فيُقصّ عند حدود `viewer-stage` أو الغلاف بدل أن يمدد الواجهة. كما أن `.student-chat-panel` يحتوي داخليًا على رأس الدردشة وسجل الرسائل وأزرار التصوير/الرسالة، وليس عنصرًا مستقلًا يسمح للصفحة بالتمدد.

النتيجة: تغيير `height` وحده لا يغيّر الارتفاع المرئي؛ يجب أولًا إعادة تصميم تخطيط الوضع العمودي بحيث يخصص 400px فعليًا للدردشة، أو يسمح بالتمرير/التمدد المقصود، مع إعادة حساب مساحة الفيديو. لم يتم تعديل الكود في مرحلة التشخيص هذه.
